---
title: "dplyr vs pandas"
description: |
  A short description of the post.
author:
  - name: dondon
    url: {}
date: 2021-12-24
output:
  distill::distill_article:
    self_contained: false
---



```{r}
library(reticulate)
```


```{r, eval = F}
reticulate::conda_create(envname = "blog", python_version = "3.8.8")
```

```{r}
reticulate::use_condaenv("r-reticulate", required = T)
py_config()
```


```{r, eval = F}
reticulate::conda_install(envname = "r-reticulate", packages = "pandas")
reticulate::conda_install(envname = "r-reticulate", packages = "numpy")
reticulate::conda_install(envname = "r-reticulate", packages = "scikit-learn")


```

```{python}
import pandas as pd
import numpy as np
from sklearn.ensemble import RandomForestClassifier

clf = RandomForestClassifier(random_state=0)
X = [[ 1,  2,  3],
     [11, 12, 13]]
y = [0, 1]  
clf.fit(X, y)
clf.predict(X)
```


추가.. 

# 참고자료

<https://www.r-bloggers.com/2020/04/how-to-run-pythons-scikit-learn-in-r-in-5-minutes/>


